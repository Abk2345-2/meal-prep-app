{
  "name": "pantrytoplate",
  "private": true,
  "version": "0.1.0",
  "description": "PantryToPlate — turn what you have into meals. Monorepo root.",
  "workspaces": [
    "packages/*",
    "web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "build:shared": "npm run build --workspace @pantrytoplate/shared"
  }
}
